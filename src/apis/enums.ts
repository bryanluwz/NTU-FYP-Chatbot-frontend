export enum ChatUserTypeEnum {
  System = "system",
  User = "user",
  AI = "ai",
}

export enum UserRoleEnum {
  Admin = "admin",
  Educator = "educator",
  User = "user",
}

export enum TabEnum {
  Chat = "chat",
  Dashboard = "dashboard",
}
